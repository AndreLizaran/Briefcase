// General
export const container = 'p-6 pt-20 w-full';

// Buttons
export const button = 'px-4 py-2 rounded text-white font-semibold';
export const redButton = `${button} bg-red-500 hover:bg-red-400`;
export const whiteButton = `${button} bg-white hover:bg-gray-200 text-neutral-800`;
export const borderWhiteButton = `${button} border-2 border-white`;
export const blueButton = `${button} bg-blue-600 hover:bg-blue-500`;
export const navButton = `${button} hover:bg-gray-600`;

// Headers
export const sectionTitle = 'text-xl mb-1 text-gray-300';
